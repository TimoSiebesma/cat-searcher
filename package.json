{
  "name": "cat-searcher-notifier",
  "version": "1.0.0",
  "description": "Vercel serverless notifier for new adoptable cats",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel",
    "test:local": "ts-node --esm test-local.ts"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.1",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "vercel": "^33.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
